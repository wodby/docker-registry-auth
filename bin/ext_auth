#!/usr/bin/env bash

set -e

if [[ -n "${DEBUG}" ]]; then
    set -x
fi

read u p

curl "${REGISTRY_AUTH_CALLBACK}?user=${u}&password=${p}"
curl "${REGISTRY_AUTH_CALLBACK}?user=${u}&password=${p}" > /tmp/test