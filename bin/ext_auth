#!/usr/bin/env bash

if [[ -n "${DEBUG}" ]]; then
    set -x
fi

read user password

curl -s -o /dev/null -i -w '%{http_code}' "${REGISTRY_AUTH_CALLBACK_AUTH}?user=${user}&password=${password}" | grep -q 200